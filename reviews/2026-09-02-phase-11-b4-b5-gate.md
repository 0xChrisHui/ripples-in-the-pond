# Phase 11 B4/B5 Gate 清单

> 日期：2026-09-02（Asia/Shanghai）
> 范围：`/score/[id]` 生命周期、故障与 WebGL fallback、旧路径清理、OG/海报/canonical
> 基线：`playbook/phase-11/20-b-score-decoder-page.md`
> 规则：`自动` 项必须有命令输出；`前台` 项必须在真实浏览器前台完成，截图不能替代声音、焦点、滚动与 context lost 判断。

## 最终迁移更新（覆盖下文迁移快照）

> **裁决更新：下方“当前裁决”及各表格中的阻塞描述，是 B3 迁移期间保留的审计快照，已被本节覆盖，不再代表当前代码状态。** 旧正文仍保留作为迁移轨迹；关闭 Track B 时只采用本节、最新命令输出与复拍证据。

### 已完成的自动 Gate

2026-09-02 对当前工作树重新执行以下三条命令，均以退出码 `0` 完成：

```powershell
node .tmp/p11-b45-static-gate.mjs --strict
npx tsx .tmp/p11-b4-gateway-test.ts
npx tsx .tmp/p11-b2-engine-test.ts
```

- 严格静态 Gate：**15/15**。七状态、not-found、metadata 安全错误、双网关、WebGL fallback 接线、响应式 capability、无 iframe/旧 ScorePlayer/旧 events API 消费、全局播放器互斥、单 PondGL、静态 OG/海报、数字 Token canonical、永久 Decoder 合同全部通过。
- 网关测试：`singleGatewayFallbackCalls=2`、`dualGatewayFailureCalls=4`、`oversizedJsonRejected=true`；证明单网关失败会切换、双网关失败有界结束、超限 JSON 被拒绝。
- Audio/P9 测试：`contextCount=1`，覆盖 load → ready → play → pause → resume → ended → replay → destroy；旧平铺、v1、v2 三种 sounds map 均通过。

### Mainnet Token #1 前台证据与边界

OP Mainnet Token #1 已真实进入 `healthy/playing`，并留存四张前台截图：

- `reviews/evidence/p11-score-production/mainnet-token-1-375-idle.png`
- `reviews/evidence/p11-score-production/mainnet-token-1-375-playing.png`
- `reviews/evidence/p11-score-production/mainnet-token-1-1440-idle.png`
- `reviews/evidence/p11-score-production/mainnet-token-1-1440-playing.png`

375 档证据为 **375×812**；同宽 DOM 探针记录 `scrollWidth=375`、`overflowX=false`、账本 `12` 行、`BottomPlayer=false`。截图中首屏身份、分享、唱片/日食与播放控制均可见，idle 不挂小球，playing 进入单球水塘。该 DOM 数值来自较早一次同宽故障探针，健康态由随后两张 375 截图证明；二者不得合并宣传成一次完整自动通过。

故障证据现状：

- `mainnet-token-1-1440-context-lost.png` 证明 context lost 后页面没有白屏，分享、静态唱片、暂停位置与“继续播放”仍保留；它是 fallback 可见证据，不替代连续音频与同一 Canvas 恢复的人工判断。
- `mainnet-token-1-resource-failure.png` 证明单项永久资源失败被 fallback 吸收后仍能保留可读页面与操作入口；它不等价于“双网关同时失败”的最终错误文案目验，后者目前由有界网关脚本覆盖。
- `mainnet-token-1-audit.json` 是修复前的失败探针，最终状态为 `failed`，只保留为诊断材料，不得当作当前 Mainnet 健康态报告。

### 待复拍与无效证据

- 现有 playing 截图暴露了日食核心黑盘偏位/黑圈异常，已登记为 `docs/ERRORS.md` 的 **E045**。代码先把真实 `GlEclipse` 提到不受 `transform` 祖先影响的 Score 页面根层，随后修正第二层原因：共享日食/P9 的 `z=21–23` 低于 Score hero 的 `z=30`；现在由 Score 专属、无变换的 `z=35` stacking context 托起，仍低于 `z=40–42` 的身份与播放控制。最终完整 `scripts/verify.sh`、Audio/P9 与静态 15/15 均通过；**仍需复拍 playing 截图确认黑盘对位后，才能关闭这项可见 Gate**。
- 本轮复拍尝试记录在 `mainnet-token-1-eclipse-final.json`：主网关返回 `ERR_CONNECTION_CLOSED`，Permagate 返回 `404`，播放资源未 ready，故没有制造假截图或把失败探针宣传为视觉通过。该结果是外部永久资源可用性阻塞，不推翻此前 Mainnet Token #1 的 healthy/playing 证据。
- `sepolia-token-24-audit.json` 没有获得有效 Score 状态，且生产 Mainnet fallback 不应跨链读取 OP Sepolia。Token #24 只可作为隔离历史 fixture，**不是第二枚 Mainnet Token，也不是 Track B 的有效前台证据**。
- 当前 Mainnet 只有 Token #1 是 ready 样本；“第二枚真实历史 Mainnet Token”这一原关闭条件尚无数据条件，不应由 Sepolia #24 替代。

### 非音频矩阵补证

`p11-non-audio-matrix.json` 已补齐不依赖永久音频的浏览器证据：390 / 768 / 1024 均无横向溢出，分享入口在首屏，凭证账本为 12 行，`/score/` 不渲染 BottomPlayer；768 / 1024 为 2 个 Canvas（其中 1 个 WebGL）且 `data-gl-health=healthy`。OG 为 200 `image/png`，海报为 200 `image/png`，含一小时缓存与下载文件名。生产构建服务另行复核普通不存在路径为 HTTP 404；dev 流式响应的 HTTP 200 不再作为生产缺陷裁决。

仍未关闭的运行项：cold-start reduced-motion（动态 CDP 切换存在状态同步时序）、安全触发 app error、context restored/长时内存趋稳、音频路由往返与 E045 黑盘复拍。它们不能由静态或 idle 证据替代。

## 当前裁决

**尚不可关闭 Track B。** 本清单建立时 B3 正在迁移，静态 Gate 为 **6/15**；已确认 B1/B2 永久输入、客户端双网关、单一 PondGL 边界与共享 WebGL fallback 基础存在，但以下仍是阻塞：

1. 生产 `page.tsx` 仍消费旧 `ScorePlayer`、公开 events API，并保留 Decoder iframe。
2. `BottomPlayer` 尚未在 `/score/` 隐藏，新的 Score 客户端边界尚未停止全局播放器。
3. `null` 仍进入“暂时无法加载”壳，不是真正 not-found；数据库错误与不存在也尚未形成可验证区分。
4. 七个 queue key 已进入新 `ScoreLifecycle`，但文案未逐字满足 playbook，processing 控件被禁用后没有可操作的刷新入口。
5. Score 专属响应式 CSS、数字 Token canonical、静态唱片夜塘 OG/海报尚未完成。
6. `ScoreRecordAnchor` 当前把 active key 数量写入 `aria-live`；playbook 只允许播报播放、暂停、结束、错误，必须在 B3/B4 收口时移除该事件级播报。

本结论是迁移中的时间切片，不要求回滚正在编写的新组件；最终以 `--strict` 静态 Gate 和前台证据为准。

## 证据等级

| 标记 | 含义 | 通过条件 |
|---|---|---|
| 自动 | 静态扫描、类型、lint 或可控假运行时 | 命令退出码 0，JSON/终端输出归档 |
| 前台 | 真实浏览器窗口、真实 WebGL/音频/响应式 | 无控制台异常，截图 + 人工记录行为 |
| 混合 | 自动先证明结构，前台再证明体验 | 两类证据缺一不可 |

## B4-1｜七种 queue 生命周期

每种状态都使用真实 `ScorePageData` 分支，不制造百分比、不自动轮询；分享和已有凭证始终留在 DOM。

| 状态 | 必须逐字可见的主文案 | 必须保留的能力 | 方法 | 当前 |
|---|---|---|---|---|
| `pending` | 已进入作品制作队列 | 身份、创建时间、分享、手动刷新、返回池塘 | 混合 | 阻塞：当前写成“等待进入制作队列” |
| `uploading_events` | 正在保存演奏动作 | 等待、手动刷新、返回池塘 | 混合 | 阻塞：当前文案不同且按钮 disabled |
| `minting_onchain` | 正在写入 OP Mainnet | 已存在时展示完整 mint tx | 混合 | 阻塞：当前文案与能力未证明 |
| `uploading_metadata` | 正在装配永久作品 | 已存在时展示 Token | 混合 | 阻塞：当前文案不同 |
| `setting_uri` | 正在绑定永久播放器 | Token、metadata 与已存在交易 | 混合 | 阻塞：当前文案不同 |
| `success` | 永久作品已完成 | 播放、演出、分享、完整账本 | 混合 | 待 B3 前台 |
| `failed` | 作品制作没有完成 | 安全分类、现有恢复入口、既有凭证 | 混合 | 阻塞：当前文案不同，恢复路径待证 |

自动入口：

```powershell
node .tmp/p11-b45-static-gate.mjs --strict
```

该项不仅扫描七个 key，也扫描批准文案，避免“有分支但语义漂移”误判通过。

## B4-2｜不存在、数据故障与永久资源故障

| 场景 | 预期 | 自动证据 | 前台证据 |
|---|---|---|---|
| 非法 ID | 明确 not-found，不显示“稍后刷新” | `page.tsx` 进入 Next `notFound()` 或等价 404 | 404 页面有返回池塘出口 |
| 合法但不存在的数字 Token | DB miss 后查链；链上也不存在才 404 | mock/夹具证明 DB miss → chain miss | URL、标题、HTTP/页面语义一致 |
| UUID 不存在 | 404；不得宣传成永久链接 | 数据源返回 `null` | 分享/canonical 不出现 UUID 永久话术 |
| DB 抖动 + 链上成功 | `ready + degraded`，可播放 | B1 数据测试 | 页面有 degraded EditionStamp，音频可播 |
| DB 抖动 + UUID | 安全错误，不伪装 404 | 数据源错误分支 | 不白屏，有恢复出口 |
| metadata 无效/缺字段 | 保留已知身份、分享、账本，不拼当前环境资源 | B1 manifest 校验 | 安全文案；现有永久链接能打开则保留 |
| events/base/sounds 单项失败 | 唱片、身份、分享、账本仍在；提供重试与 Decoder | B2 engine `error` | 点击重试可恢复；不出现空白日食 |
| 一个网关失败 | 自动切第二网关 | `.tmp/p11-b4-gateway-test.ts` | DevTools 能看到 fallback 后成功 |
| 两个网关失败 | 有界结束并给安全错误 | 同上，两轮共 4 次调用 | 页面不无限 loading、不无限请求 |
| JSON 超过 128KiB | 拒绝解析 | 同上 | 错误态仍可读/分享 |

已通过的定向自动证据：

```text
singleGatewayFallbackCalls = 2
dualGatewayFailureCalls = 4
oversizedJsonRejected = true
```

## B4-3｜WebGL 与性能降级

| Gate | 方法 | 通过标准 | 当前 |
|---|---|---|---|
| WebGL unavailable | 混合 | 不挂 Canvas；静态唱片夜塘存在；音频/分享/账本可用 | 共享核心具备，Score 前台待验 |
| `webglcontextlost` | 前台 | `preventDefault` 后切 fallback，不白屏、不新建第二 Canvas；音频继续 | 共享核心具备，待真实事件证据 |
| `webglcontextrestored` | 前台 | 同一 Canvas 恢复，不丢控制、不重复 context | 待验 |
| React/WebGL render error | 混合 | Error Boundary 切夜塘 fallback，DOM 控件仍在 | 共享核心具备，待 Score 接线证据 |
| 低 FPS | 前台 | DPR/FBO/花瓣/pointer work 降级，永久时间线不变 | 阻塞：当前自动降级只明确覆盖 DPR |
| reduced-motion | 前台 | 空间运动/花瓣降低，播放状态和短淡入保留 | 待验 |
| coarse pointer | 前台 | 无 hover、视差、拖水面；单指滚动页面；播放/P9 保留 | 新 guard 已出现，待触屏实测 |

WebGL 前台证据至少记录：Canvas 数量、AudioContext 数量、`data-gl-health`、控制台 error、播放位置是否连续。

## B4-4｜五档视口矩阵

每个视口都测试 `ready idle → playing → paused → ended/error → 账本底部`。所有尺寸必须满足：单一纵向页面滚动、无横向滚动、首屏分享可见、44px 触控目标、账本完整值可复制。

| 视口 | 必须前台确认 | 证据名建议 |
|---|---|---|
| 375px | 单列、无球体 idle、无 pointer 动画、账本逐行、单指滚动 | `b45-375-ready.png`、`b45-375-ledger.png` |
| 390px | 长中文/英文不溢出，safe-area 不遮分享/播放 | `b45-390-ready.png` |
| 768px | 断点没有突然跳位，loading 与 Hero 尺寸接近 | `b45-768-ready.png` |
| 1024px | 桌面 capability 只在 fine pointer 开启，键盘焦点清楚 | `b45-1024-playing.png` |
| 1440px | 水塘层次、账本阅读对比、分享与主唱片不争抢 | `b45-1440-ready.png`、`b45-1440-playing.png` |

另外在 375 与 1440 各跑一次 `prefers-reduced-motion: reduce`；375 再跑 coarse pointer，1440 再跑 forced WebGL fallback。

## B4-5｜路由往返与音频互斥

必须在同一个前台标签页执行：

```text
/ 播放首页曲目 → /score/1 → 播放/暂停 Score → / → 再进入 /score/1
```

逐项勾选：

- [ ] 进入 `/score/1` 时只调用一次全局 `PlayerProvider.stop()`。
- [ ] `/score/` 不渲染 `BottomPlayer`，DOM 中也不存在隐藏的重复控制。
- [ ] Score 只创建一个路由级 AudioContext；pause/resume/replay 不重复创建。
- [ ] 离开 Score 后不恢复首页旧曲，不残留 Score 声音、RAF、timer、pointer、P9 voice。
- [ ] 返回 `/` 后 35 节点仍正常；再次进入 Score 仍只有 1 个节点和 1 个 PondGL Canvas。
- [ ] 连续往返 5 次无 context lost、无持续内存增长、无全局水位/视差残留。

自动辅助：`.tmp/p11-b2-engine-test.ts` 已覆盖 context/P9/RAF 生命周期；路由和全局播放器互斥必须前台验证。

## B5-1｜旧消费者与删除裁决

2026-09-02 快照：

| 旧项 | 当前真实消费者 | 裁决条件 | 当前 |
|---|---|---|---|
| `app/score/[id]/ScorePlayer.tsx` | 旧 `page.tsx` | 新页面通过后删除 | 阻塞，仍被 import/render |
| `app/api/scores/[id]/events/route.ts` | 旧 ScorePlayer 的 fetch | ScorePlayer 退出且全仓无消费者后再删 | 阻塞，仍被消费 |
| `src/data/score-events-source.ts` | events API；`app/score-lab/1/page.tsx` 直接调用 | 不能随 API 盲删；先决定 lab 是否保留 | 保留待裁决 |
| 旧 Decoder iframe | 旧 `page.tsx` chain fallback | 原生 ready/degraded/error 均有出口后删除 iframe | 阻塞，仍存在 |
| 永久 Decoder HTML | NFT、OpenSea/站外、P10 postMessage | 必须保留 | 自动合同通过 |
| `PlayerProvider` | 首页、录制、收藏、素材播放等 | 不删除；仅与 Score 互斥 | 保留 |
| `/api/scores/[id]/preview` | `src/data/jam-source.ts` | 与旧 events API 不同，不得误删 | 保留 |

清理前后都跑：

```powershell
rg -n "ScorePlayer|score-events-source|/api/scores/.*/events|<iframe|iframe" app src
```

只有“消费者为零”才能删文件；注释和历史文档不是运行时消费者，但应同步掉误导性的源码注释。

## B5-2｜OG、海报与 canonical

| Gate | 自动 | 前台/图像目验 |
|---|---|---|
| OG 1200×630 | 不 import WebGL/AudioContext，不出现 playing 状态 | 静态唱片浮于夜塘；标题、曲名、Token 清楚 |
| 海报 1080×1920 | 同上；二维码/URL 指向 canonical | 375 与桌面打开均可下载，二维码可扫 |
| 已铸 canonical | `/score/<tokenId>` | 页面 meta、OG URL、分享、二维码四者逐字一致 |
| processing UUID | 不声明为永久 canonical，不在 OG 文案宣传“永久作品” | 分享仅作为制作进度入口，状态清楚 |
| not-found | 不生成误导作品卡 | fallback 图与 404 语义一致 |

当前 OG/海报仍是通用黑底封面模板，不满足“静态唱片浮于夜塘”；`generateMetadata` 仍用 `score.id` 作为 OG URL 且没有数字 Token `alternates.canonical`，均为 B5 阻塞。

## B5-3｜DOM、可访问性与永久凭证回归

- [ ] 每个页面分支只有一个 `h1`；Canvas 仅装饰，身份/控制/凭证都有 DOM 等价物。
- [ ] 唱片按钮 accessible name 随 idle/loading/playing/paused/ended/error 改变。
- [ ] `aria-live` 只播报播放、暂停、结束、错误；不得播报 active key 或逐事件计数。
- [ ] 播放、暂停、分享、复制、外链均可 Tab/Enter/Space 操作，焦点不落入 Canvas。
- [ ] 完整合约、Token、holder、creator、mint tx、setURI tx、tokenURI、metadata、events、base、sounds、Decoder 同页存在。
- [ ] 短显示复制的是完整值；复制失败时完整值可手动选择，不只显示截断串。
- [ ] creator 与 currentHolder 不混名；缺失值不伪造 `0`、空哈希或 Unix epoch。
- [ ] 分享入口在 WebGL 前的 DOM 层；WebGL unavailable/context lost 时仍能操作。

## 自动命令总表

迁移期间只看报告，不以失败中断其他 agent：

```powershell
node .tmp/p11-b45-static-gate.mjs
```

B4/B5 申请关闭时执行严格 Gate：

```powershell
node .tmp/p11-b45-static-gate.mjs --strict
npx tsx .tmp/p11-b4-gateway-test.ts
npx tsx .tmp/p11-b2-engine-test.ts
npx tsc --noEmit
npx eslint app/score/[id] src/features/score-playback src/components/player/BottomPlayer.tsx
bash scripts/verify.sh
```

## Track B 关闭条件

只有以下全部满足才能把状态推进到 P11-C：

1. 静态 Gate 15/15，完整 verify 通过。
2. 七状态、not-found、metadata 错误、一个/两个网关失败、无 WebGL、context lost 均有证据。
3. 375/390/768/1024/1440 五档前台通过，375/1440 reduced-motion 通过。
4. `/ → /score/1 → /` 连续往返通过，无双播放器、幽灵声音、重复 Canvas/context/P9。
5. Token #1 与第二枚真实历史 Token 的 manifest、事件数、账本和永久 Decoder 一致。
6. 旧消费者按真实引用裁决完毕；永久 Decoder/P10 站外合同未受破坏。
7. OG、海报、canonical 四处链接一致，processing UUID 未被宣传为永久链接。
