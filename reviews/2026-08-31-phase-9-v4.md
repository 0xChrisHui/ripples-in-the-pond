# Phase 9 v4 完成验收

日期：2026-08-31  
权威规格：`playbook/phase-9/03-v4-33-key-audiovisual-remap.md`  
结论：F0–F6 工程施工与自动验收已完成，停在 **Stop V4**，等待用户第四轮集中目验。

## 1. 最终数量与映射

- 物理键固定为 `A–Z + 3–8 + Space`，共 33 键；Shift/Caps 不再产生第二层视觉身份。
- 33 个 WAV 与 33 个运行时 MP3 均为 33 个唯一 SHA-256；33 个键、音效与动画严格一一对应。
- 注册表保留 45 个历史 FX 供追溯，正式入口只启用 33 个；被放弃项只退出入口，未越权物理删除。
- 最终映射以 playbook §3.2 为准；静态审计结果为：`legacy=45 / active=33 / requiresEclipse=13 / independent=20 / moonWrite=0 / waterWrite=T,U,V`。

静态证据：`reviews/evidence/p9-v4/v4-static-audit.json`（生成器为同目录 `static-audit.ts`）。
音频特征证据：`reviews/evidence/p9-v4/v4-audio-analysis.json`。

## 2. v4 核心收口

- 无音乐时，20 个独立效果照常演奏；13 个日食依赖效果只跳过视觉，声音仍播放。进入日食后 33/33 全部可达。
- 鼠标点击音乐圆后主动释放按钮焦点，第一下按键不再出现焦点方框，也不会被按钮输入保护吞掉。
- A/D/Y 使用速度冲量包络：新声部从 0 连续积分；回落中的第一次重按只抵消负速度，后续重按才重新加速。W 保持目标值/呈现值分离。
- B 最多六批独立孢子并行且旧批次完整走完；T、V、Space 使用批次上限支持多浪/多前线叠加。
- F 的日晕颜色默认可见；I 结束即定格；J 覆盖全部微光；M 汇入后不回弹；N 使用分散渐显渐隐；R 按当前花瓣数量的比例湮灭并闪耀。
- 删除项 H/P/Z/A′/B′/C′/G′/H′/K′ 以及并入 L′ 的 N′ 已退出最终入口；Y 被保留并修复回落横跳。
- 音乐圆数字开关默认关闭；第一组音乐圆独享 12 色并集；键盘动画月光写入继续保持 0。

## 3. 浏览器实测

- 无音乐全扫：33 次输入，20 次视觉接受、13 次 `no-eclipse`；Shift+A 与 A 同为 FX01。
- 日食全扫：33 次输入、33 次视觉接受、33 个唯一 FX。
- A 在真实下降段重按：50ms 环形缩放跳变量 `0`；Y 在真实回中段重按：位移跳变量 `0 px`。
- B 可见声部 6 批；Space 可见边界声部 6 条；F 的滤镜从无色变为明确色相滤镜。
- 点击播放后的焦点为 `BODY`，outline 为 `none 0px`；圆圈数字默认关闭。
- 1440×900 / DPR 1 前台压力段平均 `56.52 fps`，`visibilityState=visible`；最长余韵后孢子与边界实例均归零。
- 390×844 / DPR 2 与 reduced-motion 路径完成截图；控制台异常 `0`。

浏览器证据：`reviews/evidence/p9-v4/v4-browser-audit.json` 与同目录 `v4-*.png`。

## 4. 工程验证与边界

- 损坏的 `.next` 已可恢复隔离到 `.tmp/p9-v4-broken-next-20260831`，没有修改生成文件。
- `node_modules` 中失效的 `pino` 依赖树已通过现有 lockfile 的 `npm ci` 重建；`package.json` 与 `package-lock.json` 未改变。
- 完整 `scripts/verify.sh` 通过：TypeScript、ESLint、代码/目录硬线、危险代码扫描、Next.js 生产构建、Forge 42/42。
- P9 v4 仍停在沙盒审阅阶段；未进入 Track C、未物理清理历史实现、未部署。

## 5. Stop V4 目验建议

用户在 `/test3` 选择任一音乐圆后，用正常演奏节奏重点检查：

1. A/D/W/Y 回落中连续重按是否符合手感。
2. B、T、V、Space 的多批并行是否明显且不互相等待。
3. F、J、M、N、R 是否达到最终审美强度。
4. 其余 33 键逐一决定“冻结”或进入下一轮小范围调参。

未收到用户第四轮目验结论前，P9 停在 Stop V4。
