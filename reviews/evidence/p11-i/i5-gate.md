# P11-I5 档案前景 Gate

日期：2026-09-24

分支：`claude/p11-i-replay`

## 结果

- I-D1 基底断言：`BASE_OK`，分支落后 `origin/main` 为 `0`。
- I5 浏览器 Gate：通过，见 `i5-archive.json`。
- TypeScript 与定向 ESLint：通过。
- 完整 `scripts/verify.sh`：通过；生产构建 38/38，Forge 56/56。
- Google Fonts：按用户授权仅在本地构建时临时移除初始化；验证后恢复 `app/layout.tsx`，无产品差异。

## 档案预备与身份边界

- 首页后台只挂载一个档案实例；进入 `/me`、返回 `/` 后仍为同一 DOM 实例，没有第二个 PondGL。
- 首页阶段档案为 `inert`、`aria-hidden` 且不可交互；档案准备完成后导航才收场，不暴露半空内容。
- 未登录样本没有发出任何 `/api/me/**` 私人请求，也没有同源写请求；退出或身份变化仍由既有身份 generation 和 abort 边界清除预备结果。
- 首页首个圆圈约 `419.5ms` 可见，档案约 `1079.4ms` 准备完成；本轮记录到 0 个长任务。缺少同机改动前自动化样本，因此只记录绝对值，不宣称相对性能百分比。

## 交互证据

- `/me` 的真实 `KeyA` 没有创建音频、草稿或首页按键状态，首页演奏键已隔离。
- `/me/test` 的水面背景输入会产生一次涟漪；按钮和滑块输入不产生涟漪，遮罩本身保持 `pointer-events: none`。
- 375×812、768×1024、1024×768、1440×900 四档均无横向溢出；档案登录入口实际触控尺寸为 `78×44px`。
- 页面错误为 0；Water Core 与两个既有 Canvas 在往返中保持同一实例。

## 已知人工缺口

浏览器使用全新未登录资料，因此 B10/B11 的真实三块档案数据、收藏整行播放、倒计时、铸造入口与有数据唱片列表只保留为早上登录后的只读目验项。自动 Gate 已覆盖未登录空态、预备时序、唯一实例、键盘隔离、控件涟漪隔离和四视口布局。

## 文件

- `reviews/evidence/p11-i/i5-archive-check.mjs`
- `reviews/evidence/p11-i/i5-archive.json`
