# Phase 9 v3 完成验收

日期：2026-08-28  
权威规格：`playbook/phase-9/02-v3-retrigger-and-material-clarity.md`  
结论：R0–R6 已完成，停在 **Stop R**，等待用户第三轮集中审阅。

## 1. 最终映射

- 注册表保留 45 个编号，实际可演奏视觉键为 42 个。
- `Z / FX26` 留空；`Shift+B / FX28`、`Shift+S / FX45` 删除且浏览器实测无视觉响应。
- `Shift+A / FX27`、`Shift+C / FX29`、`Shift+K / FX37` 保留准备删除状态；`Shift+H / FX34` 已重新激活。
- 月光写入白名单为空；水波写入仍严格只有 `FX20 / T`、`FX21 / U`、`FX32 / Shift+F`。

静态证据：`reviews/evidence/p9-v3/v3-static-audit.json`。

## 2. R0–R6 结果

### R0 真值审计

- 固定 42 键状态、三枚无视觉键、共享参数与单效参数来源。
- 参数面板所有保留项均能在 runtime 找到真实读取位置；最后补接了 FX38“边界宽度”和 FX40“前线宽度”。

### R1 重触发策略

- 42 个效果全部声明五类之一：平滑累积 24、多实例生成 7、对象轮换 5、调色板循环 3、单向提交 3。
- 平滑效果拆成目标强度与呈现强度，衰退中重按不再瞬时回峰。
- B、Shift+F 等生成独立批次并设并发上限；J/R/S 每次按键轮换真实对象；K/P 从当前插值位置继续换色；M/V/Shift+E 不倒放。

### R2 基础场景与颜色

- 音乐圆点击焦点不再出现方框；键盘可访问性保留。
- 日食模式只由当前播放圆参与花瓣遮挡，已消失圆不再挖空花瓣。
- 音乐圆数字开关默认关闭。
- 第一组扩为 12 色，35 颗球实际覆盖全部 12 色；第二、三组继续保持 8 色。
- K/P 共用 24 色闭环母版。

### R3 第一组 A–Y

- A/D/W/Y 改为连续目标追踪；B 最多六批孢子并行；J 使用独立熄灭生命周期。
- E/F/H/I/M/N/O/P/Q/R/S/T/V 按第三轮反馈重做或增强；T 使用屏外超大圆波场，V 三分裂后单向消散。
- W 的持续连击受柔和上限约束；X 的时长与退暗梯度按反馈收口。

### R4 Shift 组

- Shift+B 注销；Shift+D 为相邻花瓣缝合；Shift+E 为清楚的单向物质转化。
- Shift+F 支持多波并行并自动遍历水光配色；Shift+H/L/N 支持连续接棒。
- Shift+O 点击立即过境；Shift+P 是乳白物质膜，Shift+Q 是多条反相前线，灰度空间轮廓可区分。

### R5 参数真值

- 第三轮指定默认值与范围已写入统一元数据；FX11 的 `2s / 0.5s` 和 FX31 黑盘停留均接入 runtime。
- v3 存储键和旧值迁移已就位，新默认不会被旧 v2 localStorage 静默覆盖。
- 无读取者参数已移除或补接，面板不存在“能拖但无视觉作用”的已知项。

### R6 自动验收

- HUD：`42 / 42`；三枚无视觉键保持静默。
- A 衰退中重按 50ms 跳变量 `-0.0391`，没有快速弹回最大值。
- B 连击可见 6 组孢子；Shift+F、Shift+P、Shift+Q、T 均完成独立截图。
- 焦点 outline：`none 0px`；音乐圆数字标签数：`0`。
- 压力演奏后：`60.28 fps`，PerfHUD `60 fps · max 19ms`，页面 `visibilityState=visible`。
- 390×844 / DPR 2、reduced-motion、最长余韵恢复均完成；控制台异常 `0`。
- 清理损坏的 Next.js 生成缓存后，`scripts/verify.sh` 全通过：TypeScript、ESLint、代码/目录硬线、危险代码扫描、Next.js 生产构建、Forge 42/42。
- 浏览器证据：`reviews/evidence/p9-v3/v3-final-browser-audit.json` 与同目录 `v3-final-*.png`。

## 3. 工程边界

- P9 v3 仍由 `/test3` 沙盒门控；未自动提升到默认首页、`/test4` 或生产部署。
- 未进入 Track C；准备删除效果的历史实现暂时保留，等待用户审阅后统一清理。
- 自动验收确认机制、并发、稳定性与关键轮廓；每个效果最终审美仍由 Stop R 的人工连续演奏决定。

## 4. Stop R 审阅建议

用户在 `/test3` 先选一首音乐圆，再按正常演奏节奏检查：

1. A/D/W/Y 的衰退中重按是否仍显得自然。
2. B/J/R/S/Shift+F 的每次点击是否都能看见独立反馈。
3. M/V/Shift+E 的单向物质因果是否清楚。
4. T、Shift+P、Shift+Q 的空间结构是否符合想象。
5. 最后决定 FX27、FX29、FX37 是删除、重做还是保留。

未收到“进入 Track C / 定稿”前，P9 停在这里。
