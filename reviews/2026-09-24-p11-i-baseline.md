# P11-I I0：main 效果基准表（2026-09-24）

## 基底与取证条件

- 工作树：`E:\Projects\nft-music-p11-i`；分支 `claude/p11-i-replay`；`origin/main=9324386`，I-D1 断言通过、落后 0。
- 对照树：`E:\Projects\nft-music-progress-release`，`HEAD=origin/main`，工作区干净。两棵树均独立 `npm ci` 成功、使用同一份未入库的 `.env.local`。
- 本地公开配置：OP Sepolia（链号 11155420，ScoreNFT `0xE0fAed842283F3D689AA8619CBb0ccc232A1DB23`）；音频镜像基址已设置。本地连接生产 Supabase，只做只读浏览器操作。
- 正式站点 `https://pond-ripple.xyz` 的 `/score/1` 返回 HTTP 200，响应服务器为 Vercel；无法从公开响应确认部署提交恰好是 `9324386`，因此线上取证只作外部参照，不冒充精确同提交证明。
- 证据：`reviews/evidence/p11-i/baseline/main-reference.json` 是本地 main 24 组路由/视口采样；`swiftshader/` 是可见水面的本地截图；`production-webgl/main-reference.json` 是正式站点 24 组采样；`production-webgl-score-retry/` 记录真实指针点击后的播放等待；`home-dynamic-fresh/` 记录首页演奏重试。所有截图为 JPEG，总体积受控。
- 本机 Google Fonts 请求超时，两棵本地树均使用回退字体。普通无界面 Edge 的 GL health 为 `unavailable`；启用软件 WebGL 的独立 Edge 曾取得可见水面截图，但连续采样后也回到不可用状态。该设备状态不用于判断产品视觉回归。

## 18 项基准

“预期”沿用 `playbook/phase-11/90-i-mainline-replay.md` §5；这里同时记录实际可见证据与缺口。动态项要求的 0/150/450/1500ms，未取得时逐项说明。

| ID | 路由与操作 | main 预期与现有证据 | 状态 / 证据路径 | 来源 |
|---|---|---|---|---|
| B01 | `/` 冷启动 | 加载提示后水面接管；软件 WebGL 下可见 35 圆和水面，普通无界面模式落入黑色降级。冷启动短暂编译画面有截图。 | 部分；`swiftshader/home-1440.jpg`、`home-375.jpg` | `05e7e9a`、`59aec3d` |
| B02 | `/` 圆圈/分组/拖放 | 稳定画面可见多色音乐圆和分组入口。悬停、拖放与点击播放未取得：重试时持续显示“音效加载中”，无可交互圆。 | 部分；`swiftshader/home-1440.jpg`、`home-dynamic-fresh/main-reference.json` | P8、P11 |
| B03 | `/` 水面与指针 | 水面、花瓣、月光在稳定截图中可见；鼠标涟漪、拖球尾迹、滚轮水位因后续 GL 不可用未取得时间序列。 | 部分；`swiftshader/home-1440.jpg` | P8 |
| B04 | `/` 播放日食 | 合同为播放球聚焦、其他圆隐去，暂停/结束归位；真实点击阶段卡在音效加载，0/150/450/1500ms 均无可信播放态截图。 | 未取动态；`home-dynamic-fresh/main-reference.json` | P8、P11 |
| B05 | `/` P9 键盘 | 合同为日食态 33 键响应，非日食态拒绝；未进入播放态，键盘效果未取。 | 未取动态；`home-dynamic-fresh/main-reference.json` | P9 v4.2 |
| B06 | `/` #36 访客 | 合同为访客出现/退场、ECHO 日食恢复；本地 Sepolia 配置与正式站点状态均未在采样窗口触发 #36，四时间点不可采。不能据此认定回归。 | 环境/触发缺口；`production-webgl/home-1440.jpg` | `327c40c`、`2839200`、`c8e59d2` |
| B07 | `/` 导航/登录 | 桌面可见“池中回声”、艺术家及登录；375px 可见主菜单。未点击注册入口，避免任何写入。 | 静态可见；`swiftshader/home-1440.jpg`、`home-375.jpg` | `d35ec0f`、`3dddf52` |
| B08 | 全局播放器 | 合同为底部播放与拖动；进入 Score 的停止时机是 G/H 已批准差异。未能启动首页播放，因此行为需后续复验。 | 待播放环境；`home-dynamic-fresh/main-reference.json` | `9324386` |
| B09 | `/me` 未登录 | 四视口均能打开未登录档案状态，没有白屏或横向溢出。 | 已取；`-me-375.jpg` 等、`main-reference.json` | P11-C |
| B10 | `/me` 私人区域 | 三块音乐区域、涟漪、倒计时、收藏行播放和铸造入口都需要真实登录态；本轮浏览器无登录态，未点铸造。 | 待用户只读目验；`production-webgl/-me-1440.jpg` | `8523600` 等 |
| B11 | `/me` 唱片列表 | 同 B10；本地 main 还可能因复合外键歧义报错，I1-a 会修。不能在未登录画面判定列表是否正常。 | 待用户只读目验；`production-webgl/-me-1440.jpg` | `8ce2514` |
| B12 | `/score/1–4` 首屏 | 正式站点四枚 Token 均显示 ready 唱片、分享和凭证区；本地 Sepolia 配置显示 failed，只能用线上作外部参照。 | 部分；`production-webgl/-score-1-1440.jpg` 至 `-score-4-1440.jpg` | `47a1a19` 等 |
| B13 | `/score/1–4` 日食 | 正式站点 #1 唱片真实指针点击后进入“已记录播放请求”，45 秒内未转 playing；GL health 同时为 `unavailable`。因此 0/150/450/1500ms 日食对照和肉眼确认均未完成；这是 I6 的硬缺口。 | 未取动态；`production-webgl-score-retry/main-reference.json`、`score-after-click-unavailable.jpg` | `fba765a`、`d53d715` |
| B14 | `/score/1–4` 音频/P9 | #1 点击排队文案可见，永久音频未在 45 秒内就绪；流式底曲、首声与 P9 未取。 | 部分；`production-webgl-score-retry/main-reference.json` | `67fa3fc` 等 |
| B15 | `/score/1–4` 阅读 | 四枚正式 Token 的凭证区开头可见、页面无横向溢出；滚动、复制未在自动采样中操作。 | 部分；`production-webgl/-score-1-1440.jpg` 等 | P11 滚动热修 |
| B16 | `/score/1–4` 返回 | main 首屏按钮文字“返回池塘”，目标合同为首页；G/H 会有意改为档案。未点击，路径由当前代码和链接结构核对。 | 静态可见；`production-webgl/-score-1-1440.jpg` | P11-B |
| B17 | 全部能力 | 375/768/1024/1440 四视口均完成导航采样；无 GL 兜底可见，reduced-motion 未单独采样。 | 部分；本地及线上两份 `main-reference.json` | P11-F |
| B18 | 全部错误/溢出 | 两份 24 组自动采样均无页面异常事件、均无横向溢出；首次编译字体回退警告和外部音频等待单列，不把它们说成产品通过。 | 自动通过当前采样；两份 `main-reference.json` | — |

## I0 Gate 与后续用法

- 18 项均有证据或无法取得的具体原因；本步没有产品代码改动。
- **基准表尚未获用户逐条确认**。按夜间授权继续施工；早上请优先用真实桌面浏览器确认 B13，再对 B02–B06 的动态表现及 B10/B11 私人档案补证。
- I1-b 将把数字 Score 固定为 OP 主网身份，之后本地 Score 才能与正式 Token 对齐；不得把本地 Sepolia 的 failed 状态当作视觉基准。
